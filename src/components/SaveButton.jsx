import React from 'react'
import { Box, Button } from "grommet";

export const SaveButton = (props) => {
  return (
    <Box align="center" pad="medium">
      <Button label="Save" onClick={() => {}} {...props} />
    </Box>
  );
}
